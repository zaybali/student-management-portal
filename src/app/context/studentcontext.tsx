import { createContext } from "react";

 
 const mainContext = createContext(null);

 export default function Studentcontext({children} : {children: React.ReactDOM}) {
  let studentData = [
    {
      
    }
  ]
 }